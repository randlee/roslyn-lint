# sc-lint-roslyn-demagic Package Usage

## Purpose

Phase B uses this document as the canonical analyzer package-reference guide
for the shipped `sc-lint-roslyn-demagic` package.

## Required Published Content

- package id: `sc-lint-roslyn-demagic`
- reference example:

```xml
<ItemGroup>
  <PackageReference Include="sc-lint-roslyn-demagic" Version="$(Version)" />
</ItemGroup>
```

- supported target frameworks: must match the shipped analyzer package target
- repository/project URL: must point to the correct GitHub repository
