name: Publish Roslyn.DeMagic Package

on:
  push:
    tags:
      - 'v*'  # Triggers on tags like v1.0.0, v0.5.1, etc.

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'preview'

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build -c Release --no-restore -p:Version=${{ steps.version.outputs.VERSION }}

      - name: Run tests
        run: dotnet test -c Release --no-build

      - name: Pack Roslyn.DeMagic analyzer
        run: dotnet pack src/Roslyn.DeMagic/Roslyn.DeMagic.csproj -c Release --no-build -p:Version=${{ steps.version.outputs.VERSION }} -o ./packages

      - name: Publish to GitHub Packages
        run: dotnet nuget push ./packages/Roslyn.DeMagic.${{ steps.version.outputs.VERSION }}.nupkg --source "https://nuget.pkg.github.com/randlee/index.json" --api-key ${{ secrets.GITHUB_TOKEN }} --skip-duplicate

      - name: NuGet.org publication remains manual for Phase A
        run: echo "NuGet.org publication for the first Phase A release remains manual; see docs/releasing.md."
